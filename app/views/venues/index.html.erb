<h1>Venues#index</h1>
 <!-- <div class="container">
    <div class="row">
      <% @venues.each do |venue| %>
        <div class="col-lg-3 my-3">
          <div class="card md-4" >
            <img src="<%= venue.img_url %>" class="card-img-top" alt="Listing Image">
            <div class="card-body">
              <h5 class="card-title"><%= venue.title %></h5>
              <p class="card-text">$<%= venue.daily_rate %></p>
              <%= link_to "details", venue_path(venue), class: "btn btn-outline-success" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
 # </div>-->

<!--<div class="container">
  <div class="row">
    <% @venues.each do |venue| %>
      <div class="card" style="width: 18rem;">
        <div class="">
          <img src="<%= venue.img_url %>" class="card-img-top" alt="Listing Image">
        </div>
        <div class="card-body">
          <h5 class="card-title"><%= venue.title %></h5>
          <p class="card-text">$<%= venue.daily_rate %></p>
          <a href="#" class="btn btn-primary">Details</a>
          <%= link_to "Details", venue_path(venue), class: "btn btn-outline-success" %>
        </div>
      </div>
    <% end %>
  </div>
</div>-->


<div class="container mx-auto mt-4">
  <div class="row">
    <% @venues.each do |venue| %>
        <div class="col-lg-3 my-3">
          <div class="card" style="width: 18rem;">
            <img src="<%= venue.img_url %>" class="card-img-top" alt="...">
              <div class="card-body">
                <h5 class="card-title"><%= venue.title %></h5>
                <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6>
                <p class="card-text">$<%= venue.daily_rate %></p>
                <%= link_to "details", venue_path(venue), class: "btn btn-outline-success" %>
              </div>
            </div>
        </div>
    <% end %>
        
  </div>
</div>
