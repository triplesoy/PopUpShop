<!DOCTYPE html>
<html>
  <head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>CHANGEThisToYourRailsAppName</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

<%#BEGINING OF THE NAV BAR %>
<nav class="navbar bg-dark border-bottom-dark navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <a href="<%= venues_path %>">
      <img src="<%= asset_path('popupshop-low-resolution-logo-color-on-transparent-background.png')%>" style="width: 8%; height: auto;" alt="PopUpShop">
    </a>



    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= new_venue_path %>">List my venue</a>
        </li>
        <!-- ROOM FOR ANOTHER LINK IF NEEDED -->
        <!-- <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">AnotherMenu</a>
        </li> -->
      </ul>

      <% if user_signed_in? %> <!-- Added this if statement -->
        <ul class="navbar-nav mb-2 mb-lg-0 "> <!-- Added this ul element -->
          <li class="nav-item dropdown px-4">
            <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Hi <%= current_user.first_name %>!
              <% if current_user.profile_img_url.present? %>
                <img src="<%= current_user.profile_img_url %>" alt="Profile Picture" class="rounded-circle" style="width: 40px; height: 40px;">
              <% else %>
                <img src="<%= asset_path('profile-icon-9.png') %>" alt="Profile Logo" class="rounded-circle" style="width: 40px; height: 40px;">
              <% end %>
            </a>
            <ul class="dropdown-menu ">
              <li><a class="dropdown-item text-white" href="<%= edit_user_registration_path %>">Profile</a></li>
              <li><a class="dropdown-item text-white" href=" <%= my_bookings_path %>">My Bookings</a></li>
              <li><a class="dropdown-item text-white" href="#">My Venues</a></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        </ul>
      <% else %>
        <div class="navbar-nav mb-2 mb-lg-0" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active px-4" aria-current="page" href="<%= new_user_session_path %>">Sign-in</a>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<%# END OF THE NAV BAR %>



    <%= yield %>
        <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>

  </body>
</html>
